import './Header.sass'
import MenuButton from "../../../../shared/ui/menu/MenuButton";

const Header = () => {
  return (
    <header className={'header'}>
      <MenuButton />
    </header>
  )
}

export default Header
