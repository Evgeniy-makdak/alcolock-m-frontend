import MenuButton from '../../../../shared/ui/menu/MenuButton';
import './Header.sass';

const Header = () => {
  return (
    <header className={'header'}>
      <MenuButton />
    </header>
  );
};

export default Header;
